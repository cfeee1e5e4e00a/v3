import { FC } from 'react';
import { Outlet } from 'react-router-dom';

export const BaseLayout: FC = () => {
    return (
        <>
            <header>cfeee1e5e4e00a</header>
            <Outlet />
            <footer>{'<3'}</footer>
        </>
    );
};
